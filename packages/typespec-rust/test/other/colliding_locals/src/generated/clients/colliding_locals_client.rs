// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    CollidingLocalsClientCollidesWithCoreReqOptions,
    CollidingLocalsClientCollidesWithEverythingOptions,
    CollidingLocalsClientCollidesWithOptionalRequestOptions,
    CollidingLocalsClientCollidesWithPathAndUrlOptions,
    CollidingLocalsClientCollidesWithRequestOptions,
    CollidingLocalsClientListWithCollisionsPageOptions,
    CollidingLocalsClientListWithCollisionsPagesOptions, WidgetPage, WidgetPages,
};
use azure_core::{
    credentials::TokenCredential,
    error::CheckSuccessOptions,
    fmt::SafeDebug,
    http::{
        pager::{PagerContinuation, PagerResult, PagerState},
        policies::{auth::BearerTokenAuthorizationPolicy, Policy},
        ClientOptions, Method, NoFormat, Pager, Pipeline, PipelineSendOptions, RawResponse,
        Request, Response, Url, UrlExt,
    },
    json, tracing, Result,
};
use std::sync::Arc;

#[tracing::client]
pub struct CollidingLocalsClient {
    pub(crate) bogus_url: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`CollidingLocalsClient`](CollidingLocalsClient)
#[derive(Clone, Default, SafeDebug)]
pub struct CollidingLocalsClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl CollidingLocalsClient {
    /// Creates a new CollidingLocalsClient, using Entra ID authentication.
    ///
    /// # Arguments
    ///
    /// * `bogus_url` - Service host
    /// * `credential` - An implementation of [`TokenCredential`](azure_core::credentials::TokenCredential) that can provide an
    ///   Entra ID token to use when authenticating.
    /// * `options` - Optional configuration for the client.
    #[tracing::new("CollidingLocals")]
    pub fn new(
        bogus_url: &str,
        credential: Arc<dyn TokenCredential>,
        options: Option<CollidingLocalsClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let bogus_url = Url::parse(bogus_url)?;
        if !bogus_url.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{bogus_url} must use http(s)"),
            ));
        }
        let auth_policy: Arc<dyn Policy> = Arc::new(BearerTokenAuthorizationPolicy::new(
            credential,
            vec!["https://contoso.com/.default"],
        ));
        Ok(Self {
            bogus_url,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                vec![auth_policy],
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.bogus_url
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.collidesWithCoreReq")]
    pub async fn collides_with_core_req(
        &self,
        request: &str,
        core_req: &str,
        options: Option<CollidingLocalsClientCollidesWithCoreReqOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if core_req.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter core_req cannot be empty",
            ));
        }
        if request.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter request cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        let mut path = String::from("/colliding/{request}/{coreReq}");
        path = path.replace("{coreReq}", core_req);
        path = path.replace("{request}", request);
        url.append_path(&path);
        let mut core_req_0 = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut core_req_0,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.collidesWithEverything")]
    pub async fn collides_with_everything(
        &self,
        request: &str,
        core_req: &str,
        path: &str,
        url: &str,
        options: Option<CollidingLocalsClientCollidesWithEverythingOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if core_req.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter core_req cannot be empty",
            ));
        }
        if path.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter path cannot be empty",
            ));
        }
        if request.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter request cannot be empty",
            ));
        }
        if url.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter url cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url_var = self.bogus_url.clone();
        let mut path_var = String::from("/colliding/{request}/{coreReq}/{path}/{url}");
        path_var = path_var.replace("{coreReq}", core_req);
        path_var = path_var.replace("{path}", path);
        path_var = path_var.replace("{request}", request);
        path_var = path_var.replace("{url}", url);
        url_var.append_path(&path_var);
        let mut core_req_0 = Request::new(url_var, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut core_req_0,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.collidesWithOptionalRequest")]
    pub async fn collides_with_optional_request(
        &self,
        options: Option<CollidingLocalsClientCollidesWithOptionalRequestOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        url.append_path("/colliding/optional/request");
        let mut core_req = Request::new(url, Method::Post);
        if let Some(request) = options.request.clone() {
            core_req.insert_header("content-type", "application/json");
            core_req.set_body(request);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut core_req,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.collidesWithPathAndUrl")]
    pub async fn collides_with_path_and_url(
        &self,
        path: &str,
        url: &str,
        options: Option<CollidingLocalsClientCollidesWithPathAndUrlOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if path.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter path cannot be empty",
            ));
        }
        if url.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter url cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url_var = self.bogus_url.clone();
        let mut path_var = String::from("/colliding/{path}/{url}");
        path_var = path_var.replace("{path}", path);
        path_var = path_var.replace("{url}", url);
        url_var.append_path(&path_var);
        let mut request = Request::new(url_var, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.collidesWithRequest")]
    pub async fn collides_with_request(
        &self,
        request: &str,
        options: Option<CollidingLocalsClientCollidesWithRequestOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if request.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter request cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        let mut path = String::from("/colliding/{request}");
        path = path.replace("{request}", request);
        url.append_path(&path);
        let mut core_req = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut core_req,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.listWithCollisionsPage")]
    pub fn list_with_collisions_page(
        &self,
        request: &str,
        core_req: &str,
        path: &str,
        url: &str,
        options: Option<CollidingLocalsClientListWithCollisionsPageOptions<'_>>,
    ) -> Result<Pager<WidgetPage>> {
        if core_req.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter core_req cannot be empty",
            ));
        }
        if path.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter path cannot be empty",
            ));
        }
        if request.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter request cannot be empty",
            ));
        }
        if url.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter url cannot be empty",
            ));
        }
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url_var = self.bogus_url.clone();
        let mut path_var = String::from("/list/page/colliding/{request}/{coreReq}/{path}/{url}");
        path_var = path_var.replace("{coreReq}", core_req);
        path_var = path_var.replace("{path}", path);
        path_var = path_var.replace("{request}", request);
        path_var = path_var.replace("{url}", url);
        url_var.append_path(&path_var);
        Ok(Pager::new(
            move |_: PagerState, pager_options| {
                let mut core_req_0 = Request::new(url_var.clone(), Method::Get);
                core_req_0.insert_header("accept", "application/json");
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &pager_options.context,
                            &mut core_req_0,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    Ok(PagerResult::Done {
                        response: rsp.into(),
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("CollidingLocals.listWithCollisionsPages")]
    pub fn list_with_collisions_pages(
        &self,
        request: &str,
        core_req: &str,
        path: &str,
        url: &str,
        options: Option<CollidingLocalsClientListWithCollisionsPagesOptions<'_>>,
    ) -> Result<Pager<WidgetPages>> {
        if core_req.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter core_req cannot be empty",
            ));
        }
        if path.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter path cannot be empty",
            ));
        }
        if request.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter request cannot be empty",
            ));
        }
        if url.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter url cannot be empty",
            ));
        }
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.bogus_url.clone();
        let mut path_var = String::from("/list/pages/colliding/{request}/{coreReq}/{path}/{url}");
        path_var = path_var.replace("{coreReq}", core_req);
        path_var = path_var.replace("{path}", path);
        path_var = path_var.replace("{request}", request);
        path_var = path_var.replace("{url}", url);
        first_url.append_path(&path_var);
        #[derive(serde::Deserialize)]
        struct CollidingLocalsClientListWithCollisionsPagesPage {
            #[serde(rename = "nextLink")]
            next_link: Option<String>,
        }

        Ok(Pager::new(
            move |next_link: PagerState, pager_options| {
                let url = match next_link {
                    PagerState::More(next_link) => next_link.try_into().expect("expected Url"),
                    PagerState::Initial => first_url.clone(),
                };
                let mut core_req_0 = Request::new(url, Method::Get);
                core_req_0.insert_header("accept", "application/json");
                let pipeline = pipeline.clone();
                Box::pin({
                    let first_url = first_url.clone();
                    async move {
                        let rsp = pipeline
                            .send(
                                &pager_options.context,
                                &mut core_req_0,
                                Some(PipelineSendOptions {
                                    check_success: CheckSuccessOptions {
                                        success_codes: &[200],
                                    },
                                    ..Default::default()
                                }),
                            )
                            .await?;
                        let (status, headers, body) = rsp.deconstruct();
                        let res: CollidingLocalsClientListWithCollisionsPagesPage =
                            json::from_json(&body)?;
                        let rsp = RawResponse::from_bytes(status, headers, body).into();
                        Ok(match res.next_link {
                            Some(next_link) if !next_link.is_empty() => PagerResult::More {
                                response: rsp,
                                continuation: PagerContinuation::Link(
                                    first_url.join(next_link.as_ref())?,
                                ),
                            },
                            _ => PagerResult::Done { response: rsp },
                        })
                    }
                })
            },
            Some(options.method_options),
        ))
    }
}
