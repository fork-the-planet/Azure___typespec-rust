// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    generated::models::{NamingClientClientNameOptions, NamingClientParameterOptions},
    header::clients::NamingHeaderClient,
    model::clients::ModelClient,
    property::clients::NamingPropertyClient,
    union_enum::clients::NamingUnionEnumClient,
};
use azure_core::{
    error::CheckSuccessOptions,
    fmt::SafeDebug,
    http::{
        ClientOptions, Method, NoFormat, Pipeline, PipelineSendOptions, Request, Response, Url,
        UrlExt,
    },
    tracing, Result,
};

/// Describe changing names of types in a client with `@clientName`
#[tracing::client]
pub struct NamingClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`NamingClient`](NamingClient)
#[derive(Clone, Default, SafeDebug)]
pub struct NamingClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl NamingClient {
    /// Creates a new NamingClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("Client.Naming")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<NamingClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Client.Naming.operation")]
    pub async fn client_name(
        &self,
        options: Option<NamingClientClientNameOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/client/naming/operation");
        let mut request = Request::new(url, Method::Post);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Returns a new instance of ModelClient.
    #[tracing::subclient]
    pub fn get_model_client(&self) -> ModelClient {
        ModelClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }

    /// Returns a new instance of NamingHeaderClient.
    #[tracing::subclient]
    pub fn get_naming_header_client(&self) -> NamingHeaderClient {
        NamingHeaderClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }

    /// Returns a new instance of NamingPropertyClient.
    #[tracing::subclient]
    pub fn get_naming_property_client(&self) -> NamingPropertyClient {
        NamingPropertyClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }

    /// Returns a new instance of NamingUnionEnumClient.
    #[tracing::subclient]
    pub fn get_naming_union_enum_client(&self) -> NamingUnionEnumClient {
        NamingUnionEnumClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Client.Naming.parameter")]
    pub async fn parameter(
        &self,
        client_name: &str,
        options: Option<NamingClientParameterOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/client/naming/parameter");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("defaultName", client_name);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
