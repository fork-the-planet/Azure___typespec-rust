// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use super::{
    ExportedUser, StandardClientBeginCreateOrReplaceOperationStatus,
    StandardClientBeginDeleteOperationStatus, StandardClientBeginExportOperationStatus, User,
};
use azure_core::{
    http::{
        poller::{PollerStatus, StatusMonitor},
        JsonFormat, RequestContent,
    },
    json::to_json,
    Result,
};

impl StatusMonitor for StandardClientBeginCreateOrReplaceOperationStatus {
    type Output = User;
    type Format = JsonFormat;
    fn status(&self) -> PollerStatus {
        match &self.status {
            Some(v) => PollerStatus::from(v.as_ref()),
            None => PollerStatus::InProgress,
        }
    }
}

impl StatusMonitor for StandardClientBeginDeleteOperationStatus {
    type Output = ();
    type Format = JsonFormat;
    fn status(&self) -> PollerStatus {
        match &self.status {
            Some(v) => PollerStatus::from(v.as_ref()),
            None => PollerStatus::InProgress,
        }
    }
}

impl StatusMonitor for StandardClientBeginExportOperationStatus {
    type Output = ExportedUser;
    type Format = JsonFormat;
    fn status(&self) -> PollerStatus {
        match &self.status {
            Some(v) => PollerStatus::from(v.as_ref()),
            None => PollerStatus::InProgress,
        }
    }
}

impl TryFrom<User> for RequestContent<User> {
    type Error = azure_core::Error;
    fn try_from(value: User) -> Result<Self> {
        Ok(to_json(&value)?.into())
    }
}
